# Particle Life with hand-crafted axes: size and local density, the two on which a soup
# and a soliton differ independently.
name: particle_life_metrics
complex_system:
  name: particle_life
encoder: null
descriptor:
  series: [[radius, 1.0], [clustering, 5.0]]
qd:
  num_init: 1024
