# Hand-crafted descriptor instead of the learned one: the ablation that asks whether the
# VAE earns its place against two axes a human picked.
name: lenia_metrics
encoder: null
descriptor:
  series: [[mass, 10.0], [linear_velocity, 0.01]]
