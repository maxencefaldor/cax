# The null control: no mutation, a fresh random genotype every generation. Without it no
# result from the other runs means anything.
name: lenia_random
complex_system:
  name: lenia
  sample:
    strategy: noise
qd:
  sample_ratio: 1.0
