# Flow Lenia with hand-crafted axes. Mass is conserved, so localization rather than
# survival is what the descriptor has to separate.
name: flow_lenia_metrics
complex_system:
  name: flow_lenia
encoder: null
descriptor:
  series: [[concentration, 0.5], [linear_velocity, 0.01]]
qd:
  num_init: 1024
