# The random prior beat both evolved populations to the eye, so this arm keeps fresh
# prior samples flowing through the search alongside mutation.
name: particle_life_sampled
complex_system:
  name: particle_life
encoder: null
descriptor:
  series: [[radius, 1.0], [clustering, 5.0]]
qd:
  num_init: 1024
  sample_ratio: 0.5
