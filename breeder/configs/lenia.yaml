# The protocol: one Aquarium soliton, mutated for 1024 generations, with a VAE latent
# descriptor and speed as the objective. Every default is the converged setting, so this
# file states nothing but its name; the others state only how they differ.
name: lenia
